Icon-only circular control — back arrows, close buttons, row overflow, floating add.

```jsx
<IconButton name="arrow-left" label="Back" />
<IconButton name="plus" variant="solid" tone="action" size={56} label="Add expense" />
```

`variant="plain"` scales down on press; slab variants sink instead.
