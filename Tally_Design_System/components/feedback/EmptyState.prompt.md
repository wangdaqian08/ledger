First-run and empty-list states.

```jsx
<EmptyState icon="receipt" title="Nothing here yet"
  body="Add the first expense and Tally will do the maths."
  action={<Button icon="plus">Add expense</Button>} />
```

Copy stays warm and never scolds. The glyph slab is deliberately tilted -4deg — that tilt is the brand's only decorative gesture.
