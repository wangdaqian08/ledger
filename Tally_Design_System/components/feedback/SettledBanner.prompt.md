Shown at the top of a group once every balance is zero — the emotional payoff of the product.

```jsx
<SettledBanner message="You're all square" sub="Nobody owes anybody. Nice." />
```

Pops in once with the spring easing. Only one per screen, and only when the balance genuinely hits zero.
