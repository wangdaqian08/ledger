The eight Tally expense categories as a scrolling chip row.

```jsx
<CategoryPicker value={cat} onChange={setCat} />
```

Categories are Lucide glyphs, never emoji. Import `CATEGORIES` if you need the canonical list and icon mapping elsewhere.
