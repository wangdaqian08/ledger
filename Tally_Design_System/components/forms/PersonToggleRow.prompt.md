Who's in. Included avatars sit up 2px with a double ink ring; excluded ones desaturate and fade.

```jsx
<PersonToggleRow people={group.members} selected={payers} onToggle={toggle} />
```

Scrolls horizontally rather than wrapping. Always the first step of Add Expense, before the amount is split.
