Share counts, guest counts, "how many nights" — anything small and countable.

```jsx
<Stepper value={shares} onChange={setShares} suffix="×" />
```

Ranges wider than about 20 belong on a SplitBar instead.
