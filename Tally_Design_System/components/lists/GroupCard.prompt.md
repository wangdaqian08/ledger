The home screen is a stack of these.

```jsx
<GroupCard name="Osaka trip" icon="plane" hue={5} members={members} balance={-42.8} onClick={open} />
```

A settled group shows an "All square" mint badge instead of a figure — never `$0.00`.
