repo: wangdaqian08/ledger
branch: main

## Last sync

date: 2026-07-31T10:40:32Z

### Updated in this project

- Read the full repository tree at `main` — it contains only `README.md`, `LICENSE` and `.gitignore`. There is no application code, no components, no styles and no assets to recreate.
- The only product signal available upstream is the README line "web based ledger book for split bill".
- Because no UI exists in the repo, the visual language, component inventory and UI kit in this design system are an original design, not a recreation. Nothing here was derived from repository source.
- Re-sync once app code lands; the screen map below will then need rebuilding against real source files.

## Screen map

| Project screen | Built from repo files |
|---|---|
| `ui_kits/mobile-app/index.html` | (none — repository has no UI source) |
| `components/**` | (none — repository has no UI source) |
| `tokens/**` | (none — repository has no styles) |
